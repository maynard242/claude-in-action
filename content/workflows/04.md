---
num: 4
slug: "4"
title: "The Whole-Book Review, in Parallel"
capability: "Parallel subagents"
capabilityShort: "Parallel agents"
actLink: "Act 5 · Parallel execution"
timeOfDay: "Overnight · fans out across the book"
oneLiner: >-
  One agent per name, or per sector, running at once. Each returns a one-pager.
  Then a synthesis across the whole book — drift against policy, and a recommended
  trade list you approve before anything moves.
job: >-
  You can review one name well or your whole book badly. Parallel changes the math:
  fan a research agent across every position or sector overnight, each producing a
  profile, then synthesize — the common thread, the outlier, the drift from your
  policy weights. It ends in a recommended trade list. It does not trade.
whatYouSee:
  - "You hand Claude your coverage list or sector map and ask for the book-level review."
  - "It fans out — one earnings-reviewer or market-researcher per name or sector, running in parallel, not one after another."
  - "Each returns a one-pager: positioning, the last 90 days of moves, where it's vulnerable, one thing to watch."
  - "It synthesizes across all of them, checks drift vs. your target weights, and produces a recommended trade list — for your review queue, never the tape."
prompts:
  - label: "Fan a review across the whole book"
    surface: claudecode
    text: >-
      Run a book-level review. For each name on my coverage list, spawn a research
      pass in parallel — not sequentially: current positioning, the three most
      important moves in the last 90 days, where it's vulnerable, and one thing to
      watch. When all have reported back, synthesize across them: the common
      pattern, the outlier, and the single name I should bring to the morning
      meeting first.
  - label: "Drift check and a trade list (never executed)"
    surface: claudecode
    text: >-
      Compare my current book to my target policy weights. Show the drift by
      position and by sleeve, flag anything outside its band, and produce a
      recommended rebalancing trade list to bring me back to policy — tax-aware,
      with wash-sale checks. Output the trades as a list for my review. Do not
      route or execute anything.
wiring:
  - label: "Agents"
    value: "earnings-reviewer (fanned across coverage) · market-researcher (across sectors)"
  - label: "Skill"
    value: "portfolio-rebalance (wealth-management)"
  - label: "Connectors"
    value: "FactSet · S&P Global / Kensho · Daloopa"
  - label: "Runs as"
    value: "Parallel managed agents → synthesis"
  - label: "Off-ramp"
    value: "Trade list → review queue → human approval → execution elsewhere"
constraint: >-
  The bright line. Every credible deployment stops at recommendation. The agent
  drafts trades; a human approves them; execution lives in a separate, permissioned
  system. Cross that line and you've gone from research copilot to unsupervised
  trading — which is where the fiduciary and regulatory liability lives.
ahaMoment: >-
  One question got you one name. A described job got you the whole book reviewed in
  parallel — and a trade list waiting in a queue for your yes. The ceiling on what
  one PM can cover just moved. Where you stop it from moving is a choice you make on
  purpose.
---

# Workflow 4 — The Whole-Book Review, in Parallel

*Parallel subagents. Extends Act 5. Audience-facing fields above are parsed by `lib/content.ts`.
Built on the `earnings-reviewer`, `market-researcher`, and `wealth-management` plugins from
[`anthropics/financial-services`](https://github.com/anthropics/financial-services).*
