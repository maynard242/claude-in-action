---
num: 6
slug: "6"
title: "The Quant You Didn't Hire"
capability: "Code as a quant copilot"
capabilityShort: "Quant copilot"
actLink: "Act 6 · Composable agents"
timeOfDay: "Research sprint · build & validate"
oneLiner: >-
  You bring the objective and the constraints. Claude builds the optimizer — a
  factor tilt, a risk-parity weighting, a systematic rule — as inspectable,
  version-controlled code you can rerun, audit, and own.
job: >-
  Constructing a book is the quant's job: turn an objective and a set of
  constraints into weights. Claude does the build — a mean-variance or risk-parity
  optimizer, a factor model, a systematic rule — as Python you can read, version,
  and rerun. You don't get a black box. You get a model you own, with every
  assumption on the table. This is Act 6 on the desk: you stop being the user and
  become the builder.
whatYouSee:
  - "You state the objective and constraints: the universe, the target (max Sharpe, min variance, risk parity), position limits, turnover and cost assumptions."
  - "Claude writes the optimizer in Python — every line readable — pulls point-in-time data, and solves for the weights."
  - "It backtests the rule out-of-sample, reports the honest stats net of costs, and shows where it breaks."
  - "You package the working model as a reusable skill, so next quarter it's one command, not a rebuild."
prompts:
  - label: "Build a portfolio optimizer in code"
    surface: claudecode
    text: >-
      Build me a portfolio optimizer in Python I can read and version. Universe:
      [tickers or index]. Objective: [max Sharpe / min variance / risk parity].
      Constraints: long-only, max [X]% per name, sector caps at [Y]%, annual
      turnover under [Z]%. Use point-in-time prices and fundamentals from FactSet.
      Estimate the covariance with Ledoit-Wolf shrinkage. Solve for the weights,
      then show me the resulting allocation, the ex-ante risk and expected return,
      and the active bets vs. my benchmark. Write it so I can rerun it next quarter.
  - label: "Backtest a systematic rule — honestly"
    surface: claudecode
    text: >-
      Backtest this rule: [describe the signal — e.g., overweight the cheapest
      quintile on EV/EBITDA, rebalanced monthly]. Use a survivorship-free,
      point-in-time universe. Split in-sample and out-of-sample. Report returns net
      of [X] bps transaction costs, the out-of-sample Sharpe, max drawdown,
      turnover, and capacity. Tell me how many parameters I'm fitting and where the
      result is fragile. A backtest is a hypothesis, not a promise — treat it like
      one.
  - label: "Package it as a reusable skill"
    surface: claudecode
    text: >-
      Package this optimizer as a reusable skill called 'rebalance-optimizer' so I
      can run it each quarter with fresh data by name. Keep the assumptions — cost
      model, constraints, estimation method — as editable parameters at the top,
      and document them so my team can audit what's under the hood.
wiring:
  - label: "Surface"
    value: "Claude Code · Python, version-controlled"
  - label: "Skills"
    value: "skill-creator (to package it) · financial-analysis (fundamental inputs)"
  - label: "Libraries"
    value: "Python — cvxpy / PyPortfolioOpt / pandas, written and run by Claude"
  - label: "Connectors"
    value: "FactSet · S&P Global (point-in-time prices, factors)"
  - label: "Pattern"
    value: "Describe objective + constraints → Claude builds, backtests, packages the model"
constraint: >-
  Overfitting — the quant's original sin. A model with enough parameters fits any
  history and predicts nothing. Hold out data, count your parameters, charge
  realistic transaction costs and capacity, and validate out-of-sample. The
  backtest is a hypothesis, not a promise. Claude will write whatever you ask —
  including a curve-fit fantasy — so you bring the skepticism.
ahaMoment: >-
  Act 6 was the agent you designed. This is its desk version: you
  didn't run a model someone handed you — you built one, in code you can read, that
  you own and rerun. The compounding move isn't a smarter model from a vendor. It's
  a growing library of validated models you wired up yourself.
---

# Workflow 6 — The Quant You Didn't Hire

*Code as a quant copilot. Extends Act 6 — the build-it-yourself capstone of The Desk.
Audience-facing fields above are parsed by `lib/content.ts`. This is the "compose it yourself"
slot: no prebuilt optimizer ships — you build it in Claude Code and package it with
[`skill-creator`](https://github.com/anthropics/financial-services).*
