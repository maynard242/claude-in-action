# Evidence registry

Phase 1 contains no runnable harness variants and therefore no production availability evidence.

`content/evidence/` is reserved for immutable, file-backed records only after a separately authorized fixture execution, verification, recovery, and reviewer process. Test-only evidence belongs under `tests/fixtures/` and must never be represented as course availability evidence.
